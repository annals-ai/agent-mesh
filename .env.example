# Agent Bridge — environment variables
# Copy to .env and fill in your values

# Claude Code Gateway (for scripts/test-claude.mjs)
CLAUDE_GATEWAY_URL=ws://localhost:18789
CLAUDE_TOKEN=your-claude-gateway-token

# Cloudflare KV Namespace ID
# Create with: npx wrangler kv namespace create BRIDGE_KV
# Then paste the ID into packages/worker/wrangler.toml
BRIDGE_KV_NAMESPACE_ID=
