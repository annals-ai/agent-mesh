{
  "name": "agent-mesh",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "echo 'Tests moved to root scripts/e2e/ — run from project root'",
    "lint": "eslint packages/*/src/**/*.ts"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "node-datachannel", "sharp", "workerd"]
  },
  "devDependencies": {
    "commander": "^13.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.55.0"
  }
}
