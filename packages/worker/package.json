{
  "name": "@annals/bridge-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc"
  },
  "dependencies": {
    "@annals/bridge-protocol": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^3.100.0",
    "typescript": "^5.7.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
